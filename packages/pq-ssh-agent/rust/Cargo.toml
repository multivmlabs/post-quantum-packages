[package]
name = "pq-ssh-agent"
version = "0.0.1"
edition = "2021"
description = "SSH agent protocol with PQ keys"
license = "MIT"
readme = "README.md"
keywords = ["post-quantum", "cryptography", "pqc"]
categories = ["cryptography"]

[dependencies]

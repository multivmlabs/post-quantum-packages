[package]
name = "pq-cert-parse"
version = "0.0.1"
edition = "2021"
description = "Parse PQ certificates (read-only)"
license = "MIT"
readme = "README.md"
keywords = ["post-quantum", "cryptography", "pqc"]
categories = ["cryptography"]

[dependencies]
